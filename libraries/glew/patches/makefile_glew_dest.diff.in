--- src/glew/Makefile	2015-01-24 13:45:52.000000000 +0100
+++ ../Makefile_GLEW_DEST	2015-03-17 12:16:10.000000000 +0100
@@ -40,8 +40,8 @@
 $(error "Platform '$(SYSTEM)' not supported")
 endif
 
-GLEW_PREFIX ?= /usr
-GLEW_DEST ?= /usr
+GLEW_PREFIX ?= @CONTRIB_INSTALL_BASE@
+GLEW_DEST ?= @CONTRIB_INSTALL_BASE@
 BINDIR    ?= $(GLEW_DEST)/bin
 LIBDIR    ?= $(GLEW_DEST)/lib
 INCDIR    ?= $(GLEW_DEST)/include/GL
