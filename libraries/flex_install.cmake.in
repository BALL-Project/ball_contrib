MESSAGE(STATUS "Installing flex")

FILE(GLOB_RECURSE FLEX_BINARIES "@FLEX_BIN_DIR@/*")
FILE(INSTALL ${FLEX_BINARIES} DESTINATION "@CONTRIB_INSTALL_BIN@" USE_SOURCE_PERMISSIONS)

message("@FLEX_BIN_DIR@")
message("${FLEX_BINARIES}")
